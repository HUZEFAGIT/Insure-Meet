# Use Node for frontend
FROM node:18

# Set working directory
WORKDIR /app

# Copy only package.json + lock file
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy everything (including src/, public/, etc.)
COPY . .

# Expose React dev server port
EXPOSE 3000

# Start React in dev mode so hot reload works
CMD ["npm", "start"]
